import { ReferencesManagement } from "@/components/dashboard/views/ReferencesManagement";

export const metadata = { title: "Référentiels — FleetMan" };

export default function Page() {
  return <ReferencesManagement />;
}

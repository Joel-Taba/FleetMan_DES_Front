import { ManagerDashboard } from "@/components/dashboard/views/ManagerDashboard";

export const metadata = { title: "Manager — FleetMan" };

export default function Page() {
  return <ManagerDashboard />;
}

import { SubscriptionExpiredPage } from "@/components/dashboard/views/SubscriptionExpiredPage";

export const metadata = { title: "Abonnement expiré — FleetMan" };

export default function Page() {
  return <SubscriptionExpiredPage />;
}
